/** Default currency for amounts (ComprasMX data is overwhelmingly MXN). */
export const DEFAULT_CURRENCY = 'MXN';

/** OpenAI ada-002 embedding dimension. Env-overridable via EMBEDDING_DIM. */
export const DEFAULT_EMBEDDING_DIM = 1536;

/** API base path for all backend routes. */
export const API_BASE_PATH = '/api';

/** Default page size for paginated list endpoints. */
export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 100;
